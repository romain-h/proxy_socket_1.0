## Simple HTTP/1.0 Proxy.
## Romain Hardy & Lucas Personnaz

# Description
- Assignment Proxy -
This simple proxy allow HTTP request with HTTP/1.0 version. 
This program currently not support multithread and can also deal with only one request at the time.
This simple thread results with partial display of classique web page in a web browser.

# Build
This program can be compiled with the following command:
make

# Usage
./proxy <port> 
Options: -v : Verbose mode.


